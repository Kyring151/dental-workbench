# 齿案台 · 云端配置指南（免费加密云存储 · Cloudinary）

把工作台的「本地模式」升级为「云端模式」：
- **图片** 上传到 Cloudinary（大图不受限，国内可访问）
- **病历数据** 端到端加密后存入 Cloudinary 的一个固定 raw 文件 `dental_vault.json`
- **密码就是钥匙**：数据密文上云，Cloudinary 也无法解读

> 方案演进：Supabase（美国区被国内断连）、LeanCloud（停止注册）、JSONBin
> （国内网络时通时断）均已弃用。现统一使用**唯一实测国内稳定可达**的
> Cloudinary 承载图片与数据。

---

## 一、Cloudinary 上传预设确认（仅需确认一次）

1. 打开 Cloudinary 控制台 → **Settings → Upload → Upload Presets**
2. 找到 `Kyring` 预设 → **Signing mode** 必须是 **Unsigned**
   - 若是 Signed：改成 **Unsigned** → **Save**
3. `config.js` 已填好 `trolahpr` / `Kyring`，改好预设后**无需重新部署**

## 二、首次使用：创建云端数据

1. 打开站点 `https://kyring151.github.io/dental-workbench/`
2. 解锁界面 → 输入**密码**（至少 6 位）→ 点「**创建并解锁**」
   （已无需任何第三方 Key，可直接创建）
3. 弹窗会显示**同步码**（含 cloud / preset / publicId）→ **务必复制保存好**
   （换电脑/手机、或清空浏览器后用它恢复）
4. 之后正常使用：新建病例、传图、保存

> 若本地还留有旧版 JSONBin 绑定，系统会自动识别并引导重新创建，
> 旧绑定密钥会备份到本地（`dentalWorkbench.vault.jsonbin`）以免丢失。

## 三、换设备 / 清缓存后恢复

新设备打开站点 → 解锁界面 → 点「已有云端数据？用同步码绑定这台设备」
→ 粘贴**同步码** + 输入**密码** → 「绑定并解锁」。

## 四、安全与多设备

- **密码就是钥匙**：密码经 PBKDF2(10 万次) 派生 AES-256-GCM 密钥，云端只有密文，
  密码忘记无法找回
- 同步码不含密码，仅指向数据文件位置；**但请不要公开分享**
- **离线优先**：数据先安全保存在本机，云端后台异步同步；网络抖动时仍可正常使用，
  数据不丢；恢复联网后自动补同步

## 五、容量与限制

- Cloudinary 免费版：存储约 25GB、流量 25GB/月，图片与数据完全够用
- 单文件若干 MB 以内无障碍（X 光片/口内照通常 1~5MB）

## 六、常见问题

| 现象 | 处理 |
| --- | --- |
| 首次创建报「云端保存失败（4xx）」 | 预设 `Kyring` 未设为 Unsigned，去改 |
| 反复保存报「…不允许覆盖同一数据文件」 | 到 Cloudinary 该预设开启 Overwrite 后重试 |
| 「密码错误」 | 密码记错，无找回途径 |
| 「同步码不完整」 | 重新完整复制同步码（含大括号引号） |
| 想回到纯本地模式 | 清空 `config.js` 的 Cloudinary 两项再推送 |

## 七、以后如何更新

```
git add -A
git commit -m "本次改动说明"
git push
```

Pages 自动重新部署，约 1-2 分钟后线上生效。