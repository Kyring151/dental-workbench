# 齿案台 · 云端配置指南（免费加密云存储）

把工作台的「本地模式」升级为「云端模式」：
- **图片** 上传到 Cloudinary（大图不受限，国内可访问）
- **病例数据** 端到端加密后存入 JSONBin（免费、CORS 全开、国内可访问）

> 此前尝试的方案均不可行：Supabase（美国区被国内断连）、LeanCloud（已停止
> 新用户注册）、MemFireDB（无免费版）。本方案网络已实测畅通，你只需注册
> 一次 JSONBin（免费，邮箱即可），Cloudinary 你已注册。

---

## 一、Cloudinary 上传预设确认（无需再注册）

1. 打开 Cloudinary 控制台 → **Settings → Upload → Upload Presets**
2. 找到 `Kyring` 预设 → **Signing mode** 必须是 **Unsigned**
   - 若是 Signed：改成 **Unsigned** → **Save**
3. `config.js` 已填好 `trolahpr` / `Kyring`，改好预设后**无需重新部署**

## 二、注册 JSONBin 并复制 Master Key（约 3 分钟）

1. 打开 **https://jsonbin.io** → **Sign Up**（邮箱 + 密码，免费）
2. 注册后进控制台 **Dashboard**，找到 **Master Key**（一长串，形如 `$2b$10$...`）
   → 点 **复制**
3. 这是「云端钥匙」，只在你首次创建时粘贴一次；请同时抄一份保存
   （它也会包含在同步码里）

## 三、首次使用：创建云端数据

1. 打开站点 `https://kyring151.github.io/dental-workbench/`
2. 解锁界面 → 输入**密码**（至少 6 位）→ 粘贴 **Master Key** → 「创建并解锁」
3. 弹窗会显示**同步码**（含 binId 和 masterKey）→ **务必复制保存好**
   （换电脑/手机、或清空浏览器后用它恢复）
4. 之后正常使用：新建病例、传图、保存

## 四、换设备 / 清缓存后恢复

新设备打开站点 → 解锁界面 → 点「已有云端数据？用同步码绑定这台设备」
→ 粘贴**同步码** + 输入**密码** → 「绑定并解锁」。

## 五、安全说明（重要）

- **密码就是钥匙**：数据用密码加密后才上传，服务器上只有密文，密码忘记
  无法找回
- 同步码含 Master Key（写权限），**请勿公开分享**
- 每关掉标签页需重新输密码（同一标签页内刷新免密）

## 六、容量与限制

- JSONBin 免费版：单 bin 100KB（加密后），公开 bin（密文公开无妨，无密码解不开）
- Cloudinary 免费版：存储 25GB、流量 25GB/月，图片足够

## 七、常见问题

| 现象 | 处理 |
| --- | --- |
| 传图失败（400） | Cloudinary 预设 `Kyring` 未设为 Unsigned，去改 |
| 「密码错误」 | 密码记错，无找回途径 |
| 「云端创建失败（401）」 | Master Key 粘贴不完整或有空格 |
| 「同步码不完整」 | 重新完整复制同步码（含大括号引号） |
| 想回到本地模式 | 清空 `config.js` 的 Cloudinary 两项再推送 |

## 八、以后如何更新

```
git add -A
git commit -m "本次改动说明"
git push
```

Pages 自动重新部署，约 1-2 分钟后线上生效。
